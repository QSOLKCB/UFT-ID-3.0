# 2026-08-18 Research and Repository Review Action Register

**Claim class:** `DIAGNOSTIC`

Two independent deep-research reports were supplied after PR #3. They are
point-in-time diagnostic inputs, not canonical scientific authorities. Their
session-local citations are not copied into this repository.

## Findings already closed before PR #3 merged

The merged validator already covers repository-confined evidence paths, DOI
collisions across primary and alternate fields, contract-anchored status
vocabularies, complete bibliographic fields, source-work linkage, work and
claim dependency cycles, restricted established-literature exemptions,
synchronized human/machine tables, corrected snapshot dates, and mandatory
counterexample-matrix reads.

The reports' earlier recommendation not to merge the inspected head therefore
does not describe the final merged commit.

## Remaining recommendations implemented in PR #4

- pin GitHub Actions to reviewed full commit SHAs;
- use `ubuntu-24.04` and Python 3.12/3.13;
- disable persisted checkout credentials;
- compile Python before execution;
- run tests under normal and optimized Python;
- retain deterministic receipts as GitHub Actions artifacts;
- centralize finite information primitives;
- replace scientific `assert` with fail-closed exceptions;
- bound exhaustive polygon enumeration;
- add malformed-input and receipt-determinism tests;
- add a deterministic human-table renderer/checker;
- refresh the human repository map and validation quick start.

## Scientific conclusion retained as a theorem target

```text
No universal information-direction theorem exists without fixing the system,
dynamics, information functional, observation map, reference measure,
partition/coarse-graining, boundaries, and source assumptions.
```

Restricted monotonicity should be proved from the declared dynamics rather than
postulated from the word "information".

## Deliberately deferred

The proposed nested source/assessment graph schema is a breaking machine-schema
migration. It is deferred until exact source locators are added, so that the
migration occurs once rather than repeatedly.

The generic balance equation remains a model template. It will not be promoted
to predictive physics until its terms are independently operationalized and
used for held-out prediction.

## Next scientific PR

Reproduce the 2019 bit-mass arithmetic exactly, record the source locator, and
separate the numerical calculation from the additional physical premise that
an erasure-energy lower bound is intrinsic stored bit energy.
